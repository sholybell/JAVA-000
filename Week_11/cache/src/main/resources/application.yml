spring:
  redis:
    client-type: lettuce
    lettuce:  # 连接池配置
      pool:
        max-active: 8
        min-idle: 1
        max-wait: 5000
        max-idle: 5
    # Redis数据库索引（默认为0）
    database: 0
    # Redis服务器地址
    host: localhost
    port: 6379
    # 密码
    password: lansun
